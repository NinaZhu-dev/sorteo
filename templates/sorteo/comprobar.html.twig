{% extends 'base.html.twig' %}

{% block title %}Sorteo{% endblock %}

{% block body %}
    <h1>Comprobar sorteo:</h1>
    <div class="container d-flex justify-content-center w-50">
        <form method="GET" action="{{ path('app_sorteo_validar') }}" data-turbo="false">
            <div class="row">
                <div class="col">
                    <label for="fecha">Fecha</label>
                    <input type="date" class="form-control" name="fecha" id="fecha">
                </div>
            </div>
            <div class="row">
                <div class="col">
                    <label for="numero">Número</label>
                    <input type="number" class="form-control" name="numero" id="numero" placeholder="Numero">
                </div>
                <div class="col">
                    <label for="serie">Serie</label>
                    <input type="number" class="form-control" name="serie" id="serie" placeholder="Serie">
                </div>
            </div>
        <br>
        <button type="submit" class="btn btn-success">{{etiqueta_btn_enviar | default('Validar')}}</button>
        </form>
    </div>
    <div class="row">
        <div class="col d-flex justify-content-end">
            <a class="btn btn-secondary" style="margin-top:25px" href="{{ path('app_sorteo_index') }}">Volver al inicio</a>
        </div>
    </div>
{% endblock %}
